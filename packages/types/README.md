# API Types

Types shared by the web-api and app. These are used in:

- Express routes
- Angular Services such as `WebApiService`

## Conventios

In [api.ts](./src/api.ts), `fooResponseSchema` is the Zod schema object, and `FooResponse` is its TypeScript type.
